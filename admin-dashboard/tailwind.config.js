/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{ts,tsx}", // Quét tất cả file .ts và .tsx trong src
  ],
  theme: {
    extend: {}, // Tùy chỉnh theme nếu cần (màu sắc, font, v.v.)
  },
  plugins: [],
};